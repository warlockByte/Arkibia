import { cachedJson,yearOf } from './client';import type { MediaMetadata,MediaSearchResult } from '../types';
export async function searchBooks(q:string):Promise<MediaSearchResult[]>{const d=await cachedJson<any>(`https://openlibrary.org/search.json?q=${encodeURIComponent(q)}&limit=8&fields=key,title,author_name,first_publish_year,cover_i,isbn,publisher,number_of_pages_median,subject`);return d.docs.map((x:any)=>({id:x.key.replace('/works/',''),category:'books',title:x.title,subtitle:x.author_name?.[0]||'Unknown author',year:x.first_publish_year,image:x.cover_i?`https://covers.openlibrary.org/b/id/${x.cover_i}-M.jpg`:undefined}))}
export async function getBook(id:string):Promise<MediaMetadata>{const x=await cachedJson<any>(`https://openlibrary.org/works/${id}.json`);const a=x.authors?.[0]?.author?.key;let author='Unknown author';if(a){try{author=(await cachedJson<any>(`https://openlibrary.org${a}.json`)).name||author}catch{}}const desc=typeof x.description==='string'?x.description:x.description?.value;return {externalId:id,category:'books',title:x.title,creator:author,genre:x.subjects?.slice(0,4).join(', ')||'Other',year:yearOf(x.first_publish_date),image:x.covers?.[0]?`https://covers.openlibrary.org/b/id/${x.covers[0]}-L.jpg`:'',overview:desc,releaseDate:x.first_publish_date,detail:x.number_of_pages?`${x.number_of_pages} pages`:undefined}}
